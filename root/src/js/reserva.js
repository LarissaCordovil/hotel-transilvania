const button = document.getElementsByTagName('button')

const handleButton = (event) => {
    event.preventDefault()
}

button[0].addEventListener('click', handleButton)
button[1].addEventListener('click', handleButton)